{
    "name": "sample-obsidian-theme",
    "version": "1.0.0",
    "scripts": {
        "version": "node version-bump.mjs && git add manifest.json versions.json",
        "sass-dev": "sass --watch --update --style=expanded assets:.",
        "sass-prod": "sass --no-source-map --style=compressed assets:."
    },
    "devDependencies": {
        "sass": "^1.79.3"
    }
}